const {
  test,
  typecheck,
  eslint,
  build,
  updateVersion,
  publish,
} = require('../package-scripts-common');

module.exports = {
  scripts: {
    test,
    typecheck,
    eslint,
    build,
    updateVersion,
    publish,
  },
};
