const { create, publish, test, typecheck, eslint, storybook } = require('./package-scripts-common');

module.exports = {
  scripts: {
    create,
    publish,
    test,
    typecheck,
    eslint,
    storybook,
  },
};
